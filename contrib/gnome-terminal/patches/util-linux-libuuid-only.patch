diff --git a/lib/meson.build b/lib/meson.build
index 58d5da43f..890dfafe6 100644
--- a/lib/meson.build
+++ b/lib/meson.build
@@ -1,3 +1,8 @@
+if get_option('libuuid-only')
+  randutils_c = files('randutils.c')
+  md5_c = files('md5.c')
+  sha1_c = files('sha1.c')
+else
 lib_common_sources = '''
 	blkdev.c
 	buffer.c
 	canonicalize.c
@@ -106,3 +111,4 @@ lib_tcolors = static_library(
   lib_color_sources,
   include_directories : dir_include,
   dependencies : curses_libs)
+endif
diff --git a/libuuid/meson.build b/libuuid/meson.build
index a801b4e65..279237ba2 100644
--- a/libuuid/meson.build
+++ b/libuuid/meson.build
@@ -20,7 +20,7 @@ unparse_c = files('src/unparse.c')
 libuuid_sym = 'src/libuuid.sym'
 libuuid_sym_path = '@0@/@1@'.format(meson.current_source_dir(), libuuid_sym)
 
-lib_uuid = both_libraries(
+lib_uuid = static_library(
   'uuid',
   list_h,
   lib_uuid_sources,
@@ -32,7 +32,6 @@ lib_uuid = both_libraries(
   sha1_c,
   include_directories : [dir_include, dir_libuuid],
   link_depends : libuuid_sym,
-  version : libuuid_version,
   link_args : ['-Wl,--version-script=@0@'.format(libuuid_sym_path)],
   dependencies : [socket_libs,
                   build_libuuid ? [] : disabler()],
diff --git a/meson.build b/meson.build
index baca755d1..0292df4d8 100644
--- a/meson.build
+++ b/meson.build
@@ -885,6 +885,18 @@ bashcompletionslinks = {}
 
 subdir('include')
 subdir('lib')
+
+# static-everywhere: build nothing but libuuid when asked.
+#
+# subdir_done() stops interpreting this file here, which skips every
+# other library, every program, and all the man-page and completion
+# install rules below. libuuid/meson.build generates uuid.pc itself, so
+# the archive and its pkg-config file both survive the cut.
+if get_option('libuuid-only')
+  subdir('libuuid')
+  subdir_done()
+endif
+
 subdir('libblkid')
 subdir('libmount')
 subdir('libsmartcols')
diff --git a/meson_options.txt b/meson_options.txt
index f546c3ce9..3b7491243 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -37,6 +37,8 @@ option('build-libblkid', type : 'feature',
        description : 'build libblkid and many related utilities')
 option('build-libuuid', type : 'feature',
        description : 'build libuuid and uuid utilities')
+option('libuuid-only', type : 'boolean', value : false,
+       description : 'configure only libuuid and its pkg-config file')
 option('build-libmount', type : 'feature',
        description : 'build libmount')
 option('build-libsmartcols', type : 'feature',
